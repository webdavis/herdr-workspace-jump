[package]
name = "herdr-workspace-jump-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
herdr-workspace-jump-domain = { path = "../herdr-workspace-jump-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1"
